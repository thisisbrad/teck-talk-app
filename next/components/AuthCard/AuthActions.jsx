import { CardActions, styled } from "@mui/material";

const AuthActions = styled(CardActions)({
	display: 'flex',
	flexFlow: 'row nowrap',
	justifyContent: 'space-between'
});

export default AuthActions